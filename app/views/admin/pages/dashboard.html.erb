<%= render partial: "shared/heading", locals: { title: 'Admin Panel', tagline: 'Accept or Reject' } %>


<div class="px-6 lg:px-8">

  <div class="mt-8 flow-root">
    <div class="-my-2 -mx-6 overflow-x-auto lg:-mx-8">
      <div class="inline-block min-w-full py-2 align-middle sm:px-6 lg:px-8">
        <table class="min-w-full divide-y divide-gray-300">
          <thead>
            <tr>
              <th scope="col" class="py-3.5 pl-6 pr-3 text-left text-sm font-semibold text-gray-900 sm:pl-0">Applicant Name</th>
              <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-gray-900">Plan</th>
              <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-gray-900">Email</th>
              <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-gray-900">Website</th>
              <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-gray-900">Role</th>
              <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-gray-900">Newsletter</th>
              <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-gray-900">Status</th>
            </tr>
          </thead>
          <tbody class="divide-y divide-gray-200 bg-white">
            <% @user_submissions.each do |user_submission| %>
              <tr>
                <td class="whitespace-nowrap px-3 py-4 text-xs text-gray-500"><%= user_submission.name %></td>
                <td class="whitespace-nowrap px-3 py-4 text-xs text-gray-500"><%= user_submission.plan_name %></td>
                <td class="whitespace-nowrap px-3 py-4 text-xs text-gray-500"><%= user_submission.email %></td>
                <td class="whitespace-nowrap px-3 py-4 text-xs text-gray-500"><%= user_submission.website %></td>
                <td class="whitespace-nowrap px-3 py-4 text-xs text-gray-500"><%= user_submission.job_role %></td>
                <td class="max-w-s px-3 py-4 text-xs text-gray-500"><%= user_submission.text %></td>
                <td class="  py-4 text-xs text-gray-500">
                  <div>
                    <select id="location" name="location" class="mt-1 block w-full rounded-md border-gray-300 py-2 pl-3 pr-10 text-base focus:border-indigo-500 focus:outline-none focus:ring-indigo-500 sm:text-sm">
                      <option selected>Pending</option>
                      <option>Approved</option>
                      <option>Rejected</option>
                    </select>
                  </div>
                  </td>
              </tr>
            <% end %>


            <!-- More people... -->
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>
